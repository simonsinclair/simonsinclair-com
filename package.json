{
  "name": "simonsinclair-com",
  "version": "0.1.0",
  "description": "",
  "main": "pages/index.js",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "export": "npm run build && next export -o public",
    "deploy": "trash .next public && npm run export && firebase deploy"
  },
  "author": "Simon Sinclair",
  "license": "UNLICENSED",
  "dependencies": {
    "@zeit/next-css": "^1.0.1",
    "next": "^7.0.2",
    "normalize.css": "^8.0.0",
    "react": "^16.6.0",
    "react-dom": "^16.6.0"
  },
  "devDependencies": {
    "trash-cli": "^1.4.0"
  }
}
