{
  "private": true,
  "name": "simonsinclair-com",
  "version": "1.0.0",
  "description": "My website.",
  "repository": "github:simonsinclair/simonsinclair-com",
  "main": "",
  "scripts": {
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "jest --coverage",
    "test:unit:watch": "jest --watch",
    "test:e2e": "start-server-and-test serve http://localhost:5000 cypress",
    "test:e2e:open": "start-server-and-test serve http://localhost:5000 cypress:open",
    "cypress": "cypress run",
    "cypress:open": "cypress open",
    "start": "eleventy --serve",
    "prebuild": "trash public",
    "build": "eleventy",
    "serve": "firebase serve",
    "deploy": "npm run build && npm run test && firebase deploy"
  },
  "author": "Simon Sinclair",
  "license": "UNLICENSED",
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.9.0",
    "clean-css": "^4.2.1",
    "cypress": "^3.4.1",
    "eslint": "^6.4.0",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-cypress": "^2.7.0",
    "eslint-plugin-import": "^2.18.2",
    "gulp": "^4.0.2",
    "jest": "^24.8.0",
    "npm-run-all": "^4.1.5",
    "start-server-and-test": "^1.9.2",
    "terser": "^4.3.1",
    "trash-cli": "^3.0.0"
  }
}
